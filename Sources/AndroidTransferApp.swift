import SwiftUI

@main
struct AndroidTransferApp: App {
    var body: some Scene {
        WindowGroup("Android Transfer") {
            ContentView()
        }
        .defaultSize(width: 680, height: 500)
    }
}
